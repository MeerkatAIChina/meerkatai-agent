@AGENTS.md

# 项目说明

## 项目背景

这个项目是 fork 自 qm 项目，我们在 qm 的基础上进行二次开发，加入我们自己的功能。比如，做成一个桌面端产物、接入我们自己的模型等。

## 部分目录简要说明

- `docs/meerkat/`：历代二次开发的版本记录，从需求到设计再到执行，都需要记录在这里面。
- `deploy/layers/meerkat/`： Meerkat 组织专属代码的唯一合法存放地。
  - `desktop/`：全套桌面版
  - `classifier/`：隐私敏感度分类器 sidecar 服务
  - `providers/`：自定义模型供应商接入


## 项目重要守则与开发行为规范

1. 不要对 `main` 分支进行任何操作，`main` 分支唯一的作用是随主仓升级，自己的开发全部在其他分支上（比如 feature/meerkat）。
2. 为保证项目与 qm 的连续性，开发过程中尽量不要触碰到 `qm core` 内核，做到可插拔的模块化开发，**如果必须要改动内核，或触及到架构层面的修改，无论改动大小，都必须记录到对应版本的 ADR 中**。
3. 发现的 bug / 问题先建 GitHub issue 做好记录（现象、日志、定位结论），修复的 commit message 带 `(#N)` 引用，形成「issue → commit → ADR（如涉及内核）」的追溯链。
4. 对于大/难的需求，必须**走重流程开发**。大致步骤如下：
   1. 先描述需求，记录到对应版本的 `需求描述.md` 中。
   2. 对需求进行需求调研，记录到对应版本的 `需求调研.md` 中。
   3. 开展 Brainstorming 澄清需求（比如使用 superpowers）。
   4. 开始设计需求，经过讨论定稿，最终记录到 `设计文档.md` 中。
   5. 开始规划执行计划，经过讨论定稿，最终记录到 `执行计划.md` 中。
   6. 开始按照执行计划进行 TDD 开发。
5. 对于小/简单的需求，可以走敏捷方式。
