# Meerkat Skills 仓库清单 —— 每个 GitHub 仓库 = 一个 skill pack
# 格式："仓库地址|分支"（分支可省，默认 main）；新增仓库往数组里加一行即可
# core 每次导入都会现场拉取仓库最新内容，改完 skills 只需 push 后重跑脚本
# 改完后重跑：bash scripts/meerkat-local-up.sh

SKILLPACKS=(
  "https://github.com/MeerkatAIChina/meerkat-skills-triz.git|main"
)
