{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "webui-local",
  "description": "Pages served by the local web-ui process may only restart the core process (setup page proxy flow). Nothing else.",
  "windows": ["main"],
  "remote": {
    "urls": ["http://127.0.0.1:*", "http://localhost:*"]
  },
  "permissions": ["allow-restart-core"]
}
