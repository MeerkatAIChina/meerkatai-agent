{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "webui-local",
  "description": "Allow pages served by the local web-ui process (127.0.0.1/localhost, any port) to invoke app commands such as restart_core.",
  "windows": ["main"],
  "remote": {
    "urls": ["http://127.0.0.1:*", "http://localhost:*"]
  },
  "permissions": ["core:default"]
}
