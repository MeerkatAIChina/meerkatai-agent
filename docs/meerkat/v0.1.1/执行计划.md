# Meerkat v0.1.1 桌面化的沙箱 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Windows 桌面版获得 WSL2 内置沙箱（execute/write/read 工具可用，domain 白名单出网），macOS 保持降级；顺手完成优化二（锁标识）/优化三（管理员入口），定位优化一与 Issue 1。

**Architecture:** core 新增 `wsl2` sandbox backend——复用 `aws/microvm-agent/agent.mjs` HTTP 协议（core 经 WSL2 localhost 转发直连 guest agent）与 `exec-file-ops`/`exec-process-session`/`exec-backup` 辅助层；rootfs（Debian minbase + Node + agent + 工具链，≤110MB 压缩）随包内置，Tauri 壳负责 WSL2 检测/首启 UAC 启用/rootfs 导入与指纹更新；出网走 **guest 内域名白名单代理 + iptables owner 匹配 + agent 权限分离**（Task 0 spike 已验证，宿主机防火墙不需要）。

**Tech Stack:** TypeScript (core, Node ≥24.15), node:test + node:assert/strict (core 测试), Rust + Tauri v2 (壳), WSL2 (沙箱底座), Docker (仅开发机构建 rootfs 用)

## Global Constraints

- 设计文档：`docs/meerkat/v0.1.1/设计文档.md`——每个 Task 可回溯到其中一节；冲突以设计文档为准
- core 改动纯加法：`SandboxBackendName` 加值、config 枚举、wiring 分支、新文件；不改语义路由/会话锁/Skills 核心逻辑
- agent.mjs 是 aws/local 共用文件：token 认证必须 env 可选开启（`AGENT_AUTH_TOKEN`）、默认关闭，其他后端行为不变
- 代码零注释（AGENTS.md 规则）；计划示例代码中的中文注释仅供实施者理解，提交前须清除
- core 测试：`node --experimental-test-module-mocks --test test/<file>.ts`
- 组织专属内容全部在 `deploy/layers/meerkat/desktop/` 下
- 种子配置无秘密；agent token 是 core 每次启动随机生成的 per-boot 值，不落盘、不进种子
- 降级纪律：沙箱永远不是启动致命项；无沙箱 = fail-closed（execute 类工具不可用，对话/路由完整）
- 网络围栏未经验证前，**不允许实施中静默降级**为放行公网；spike 拦不死则走无网络档

---

## Phase 0: 围栏可行性 Spike

### Task 0: Win10 WSL2 直连拦截 spike（决策门）

**Files:**
- Create: `docs/meerkat/v0.1.1/spike-win10-egress.md`（结论记录：复现命令、输出、最终决策）

**Interfaces:**
- Produces: 一个布尔结论——「Win10 宿主机能否拦死 WSL2 直连 NAT 出口」。`true` → Phase 4 按分层围栏实现；`false` → Phase 4 改走无网络档（Task 11 替换为无网络档落地）

**背景**：设计文档第五节。WSL2 流量走 Hyper-V 虚拟交换机 NAT，WFP 用户态过滤规则默认管不到 VM 内部发出的包；`.wslconfig` 防火墙集成是 Win11 22H2+ 独有。本 spike 决定 Phase 4 形状，必须先于 Phase 4 完成（可与 Phase 1-3 并行）。

- [ ] **Step 1: 准备环境并验证直连基线**

Win11 22H2+（开发机）与 Win10 21H2（虚拟机即可）各一台，均启用 WSL2、导入任意 Debian 发行版。guest 内验证基线：

```bash
curl -sS -m 5 https://example.com -o /dev/null -w '%{http_code}\n'   # 期望 200（未拦前直连通）
```

- [ ] **Step 2: Win11 路径验证**

在用户级 `%USERPROFILE%\.wslconfig` 写入：

```ini
[wsl2]
firewall=true
```

然后用 Hyper-V 防火墙规则族（Win11 22H2+，真实 cmdlet 是这一对，不要找 `New-VMSwitchExtensionFirewallRule`——它不存在）拒掉 WSL 出站并重试 Step 1 的 curl：

```powershell
Set-NetFirewallHyperVVMSetting -Name '{40E0AC32-46A5-438A-A0B2-2B479E8F2E90}' -DefaultOutboundAction Allow   # WSL vSwitch 的 GUID 以 Get-NetFirewallHyperVVMSetting 实测为准
New-NetFirewallHyperVRule -DisplayName "Meerkat WSL egress deny" -Direction Outbound -Action Block -Protocol TCP
```

记录：curl 是否被拦死、`unset HTTP_PROXY` 后是否仍被拦、`.wslconfig` 改动对全机其他 WSL 发行版的副作用。

- [ ] **Step 3: Win10 路径验证（关键）**

候选机制按优先级试：
1. `vEthernet (WSL)` 接口上的 WFP 过滤器（`New-NetFirewallRule -InterfaceAlias "vEthernet (WSL*)"` 拦入站方向——WSL 出站在宿主机视角是 vEthernet 接口的入站）
2. HNS 策略（`hnsdiag` / HNS API 的 ACL policy）
3. Windows 防火墙对 WSL 宿主 IP 段的出站规则

每种记录：curl 结果、`Get-NetFirewallRule` 生效证据、是否需要管理员、重启后是否存活。

- [ ] **Step 4: 写结论文档并决策**

把三栏写进 `spike-win10-egress.md`：机制、命令序列、拦死与否。Win10 任何机制都拦不死 → 决策 = 无网络档，在设计文档第五节 spike 条款处标注结论并通知 plan owner 调整 Task 11。

---

## Phase 1: core 加法

### Task 1: `none` stub backend + 枚举加法

**Files:**
- Create: `src/sandbox/none-sandbox.ts`
- Modify: `src/sandbox/sandbox-routing.ts:17`（`SandboxBackendName` 联合类型）
- Modify: `src/config.ts:36`（Config 类型）、`src/config.ts:503-508`（`sandboxBackendEnvStrict`）、`src/config.ts:616`（production 必填校验的报错文案）
- Modify: `src/wiring.ts:607-611`（`buildBackend` Record 加 `none` 分支）
- Test: `test/none-sandbox.test.ts`、`test/config.test.ts`

**Interfaces:**
- Consumes: `Sandbox`（`src/sandbox/sandbox.ts:131`）、`AgentComputerProfile`
- Produces: `createNoneSandbox(): Sandbox`——`provision()` reject `sandbox unavailable: SANDBOX_BACKEND=none (no sandbox substrate on this machine)`；`profile = { backend: "none", writablePersistence: "resident_disk", processSessions: false, egressEnforcement: "none" }`；其余必须方法（run/readFile/writeFile/writeFileBytes/readFileBytes/listDir/removeDir/teardown）全部 reject 同一错误。wiring 导出 `buildNone` 供 Task 4 对照。

- [ ] **Step 1: 写失败测试**

```typescript
// test/none-sandbox.test.ts
import { test } from "node:test";
import assert from "node:assert/strict";
import { createNoneSandbox } from "../src/sandbox/none-sandbox.ts";

const rw = [{ scopeId: "personal:U0", mountPath: "", mode: "rw" as const }];

test("none backend rejects provision with a clear actionable error", async () => {
  const sb = createNoneSandbox();
  await assert.rejects(sb.provision(rw as never), /SANDBOX_BACKEND=none/);
});

test("none backend declares an honest empty profile", () => {
  const sb = createNoneSandbox();
  assert.equal(sb.profile.backend, "none");
  assert.equal(sb.profile.processSessions, false);
});

test("none backend rejects every operation with the same error", async () => {
  const sb = createNoneSandbox();
  await assert.rejects(sb.run({ id: "x", rootDir: "/x" }, "true"), /SANDBOX_BACKEND=none/);
  await assert.rejects(sb.listDir({ id: "x", rootDir: "/x" }, "."), /SANDBOX_BACKEND=none/);
});
```

config 测试追加（对照 `test/config.test.ts` 现有 SANDBOX_BACKEND 用例风格）：

```typescript
test("SANDBOX_BACKEND accepts none", () => {
  assert.equal(loadConfigFromEnv({ ...baseEnv, SANDBOX_BACKEND: "none" }).sandboxBackend, "none");
});
test("SANDBOX_BACKEND rejects unknown values with the full enum in the message", () => {
  assert.throws(() => loadConfigFromEnv({ ...baseEnv, SANDBOX_BACKEND: "docker" }), /aws, local, sprites, or none/);
});
```

注：拒绝用例的文案断言在 Task 4 加入 `"wsl2"` 后由 Task 4 同步更新为 `/aws, local, sprites, wsl2, or none/`。

- [ ] **Step 2: 跑测试确认失败**

Run: `node --experimental-test-module-mocks --test test/none-sandbox.test.ts`
Expected: FAIL（`createNoneSandbox is not a function` / 模块不存在）

- [ ] **Step 3: 实现**

```typescript
// src/sandbox/none-sandbox.ts
import type { Sandbox } from "./sandbox.ts";

const UNAVAILABLE =
  "sandbox unavailable: SANDBOX_BACKEND=none (no sandbox substrate on this machine)";

export function createNoneSandbox(): Sandbox {
  const fail = (): Promise<never> => Promise.reject(new Error(UNAVAILABLE));
  return {
    profile: {
      backend: "none",
      writablePersistence: "resident_disk",
      processSessions: false,
      egressEnforcement: "none",
    },
    provision: fail,
    run: fail,
    readFile: fail,
    writeFile: fail,
    writeFileBytes: fail,
    readFileBytes: fail,
    listDir: fail,
    removeDir: fail,
    teardown: fail,
  };
}
```

`src/sandbox/sandbox-routing.ts:17`：

```typescript
export type SandboxBackendName = "sprites" | "aws" | "local" | "none";
```

`src/config.ts:36` 改 `"aws" | "local" | "sprites" | "none"`；`sandboxBackendEnvStrict` 接受 `"none"`，报错文案改 `use aws, local, sprites, or none, or unset it.`；`config.ts:616` production 报错文案同步加 none。wiring `buildBackend` 加 `none: () => createNoneSandbox()` 并 import。

注：`"wsl2"` 此刻不进联合类型——它在 Task 3 随 backend 一起进，保证本 Task 编译通过。

- [ ] **Step 4: 跑测试确认通过 + typecheck**

Run: `node --experimental-test-module-mocks --test test/none-sandbox.test.ts test/config.test.ts test/sandbox-routing.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/sandbox/none-sandbox.ts src/sandbox/sandbox-routing.ts src/config.ts src/wiring.ts test/none-sandbox.test.ts test/config.test.ts
git commit -m "feat(core): add explicit none sandbox backend stub"
```

### Task 2: agent.mjs token 认证（env 可选，默认关闭）

**Files:**
- Modify: `aws/microvm-agent/agent.mjs`
- Modify: `cli/templates/aws/microvm-agent/agent.mjs`（**孪生副本，与正本逐字节相同，同一 commit 同步修改**——它是 `qm init` 打包新实例的模板（`cli/src/commands/infra.ts:105-106`），无生成脚本、手工拷贝；`cli/test/microvm-dockerfile.test.ts:7-11` 有逐字节一致性断言，只改正本会挂这个测试，它是兜底提醒而非免改理由）
- Test: `test/agent-auth.test.ts`

**Interfaces:**
- Produces: agent 行为契约——`AGENT_AUTH_TOKEN` env 非空时，`/exec` `/write` `/read` 必须携带 header `x-agent-token: <token>`，否则 401 `{ error: "unauthorized" }`；`/health` 豁免（保活探测不泄露执行能力）；env 未设置时行为与现状完全一致。Task 3 的 wsl2 backend 依赖本契约。

- [ ] **Step 1: 写失败测试**

对照 `test/local-sandbox.test.ts:35-51` 的 daemon 起法（spawn 真实 agent.mjs 于空闲端口）：

```typescript
// test/agent-auth.test.ts
import { test, before, after } from "node:test";
import assert from "node:assert/strict";
import { spawn, type ChildProcess } from "node:child_process";
import { createServer, type AddressInfo } from "node:net";
import { join } from "node:path";
import { sleep } from "../src/util/async.ts";

let daemon: ChildProcess;
let port = 0;
const TOKEN = "test-token-123";

async function freePort(): Promise<number> {
  return new Promise((res) => {
    const s = createServer();
    s.listen(0, "127.0.0.1", () => {
      const p = (s.address() as AddressInfo).port;
      s.close(() => res(p));
    });
  });
}

before(async () => {
  port = await freePort();
  daemon = spawn(process.execPath, [join(process.cwd(), "aws/microvm-agent/agent.mjs")], {
    env: { ...process.env, AGENT_PORT: String(port), AGENT_AUTH_TOKEN: TOKEN },
    stdio: "ignore",
  });
  const deadline = Date.now() + 10_000;
  for (;;) {
    try {
      if ((await fetch(`http://127.0.0.1:${port}/health`)).status === 200) return;
    } catch { /* not up yet */ }
    if (Date.now() > deadline) throw new Error("agent never became reachable");
    await sleep(100);
  }
});

after(() => { daemon?.kill("SIGKILL"); });

test("health is reachable without a token", async () => {
  assert.equal((await fetch(`http://127.0.0.1:${port}/health`)).status, 200);
});

test("exec without a token is rejected", async () => {
  const r = await fetch(`http://127.0.0.1:${port}/exec`, {
    method: "POST", headers: { "content-type": "application/json" },
    body: JSON.stringify({ cmd: "echo hi" }),
  });
  assert.equal(r.status, 401);
});

test("exec with the right token runs", async () => {
  const r = await fetch(`http://127.0.0.1:${port}/exec`, {
    method: "POST",
    headers: { "content-type": "application/json", "x-agent-token": TOKEN },
    body: JSON.stringify({ cmd: "echo hi" }),
  });
  assert.equal(r.status, 200);
  assert.match((await r.json()).stdout, /hi/);
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --experimental-test-module-mocks --test test/agent-auth.test.ts`
Expected: 后两条 FAIL（无 token 时仍 200）

- [ ] **Step 3: 实现（agent.mjs 最小改动）**

```javascript
const AUTH_TOKEN = process.env.AGENT_AUTH_TOKEN || "";

function authorized(req) {
  if (!AUTH_TOKEN) return true;
  return req.headers["x-agent-token"] === AUTH_TOKEN;
}
```

在 `createServer` 回调里、`/health` 分支之后、其余路由之前插入：

```javascript
if (!authorized(req)) return send(res, 401, { error: "unauthorized" });
```

- [ ] **Step 4: 跑测试确认通过 + 回归**

Run: `node --experimental-test-module-mocks --test test/agent-auth.test.ts test/local-sandbox.test.ts test/aws-sandbox.test.ts`
Expected: PASS（local/aws 用例不带 token 起 daemon，验证默认关闭行为不变）

- [ ] **Step 5: Commit**

```bash
git add aws/microvm-agent/agent.mjs cli/templates/aws/microvm-agent/agent.mjs test/agent-auth.test.ts
git commit -m "feat(sandbox): optional bearer token for microvm agent (AGENT_AUTH_TOKEN, off by default)"
```

### Task 3: `wsl2-sandbox.ts` backend（传输/生命周期/Sandbox 接口）

**Files:**
- Create: `src/sandbox/wsl-exec.ts`（wsl.exe 包装，可注入 fake）
- Create: `src/sandbox/wsl2-sandbox.ts`
- Create: `test/support/fake-wsl.ts`
- Test: `test/wsl2-sandbox.test.ts`

**Interfaces:**
- Consumes: `createExecFileOps` / `createExecProcessSessions` / `createExecBackup`（`src/sandbox/exec-file-ops.ts`、`exec-process-session.ts`）、`nonInteractiveShellPrefix`（`sandbox-env.ts`）、`killableScript`/`killScript`（`exec-kill.ts`）、`materializeRoLayers`（`ro-layers.ts`）、`ephemeralCredLinkScript`/`ephemeralCredLinkPaths`（`src/credentials/resident-paths.ts`）、agent token 契约（Task 2）
- Produces: `createWsl2Sandbox(workspace: WorkspaceStore, opts: Wsl2SandboxOptions): Sandbox`
  - `Wsl2SandboxOptions = { distro?: string; agentToken: string; wsl?: WslExec; fetchImpl?: typeof fetch; defaultTimeoutSec?: number; onError?: (...) => void }`
  - `WslExec = (args: string[], timeoutMs?: number) => Promise<{ code: number; stdout: string; stderr: string }>`（`spawnWslExec()` 为真实实现，测试注入 fake）
  - Task 4 的 wiring 依赖本签名；`SandboxBackendName` 加 `"wsl2"`

**设计要点**（设计文档第二节）：

- 单 distro（默认 `meerkat-sandbox`）多 scope：per-scope home `/home/qm-<slug>`，workspace = `<home>/workspace`；slug 复用 local-sandbox 的 `localSlug` 思路（清洗 + shortHash）
- core 每次启动生成 `agentToken = randomBytes(32).toString("hex")`（per-boot，不落盘）
- 生命周期：`wsl -l -q` 验 distro 存在（缺失报「rootfs 未导入」可行动错误）→ `wsl -d <distro> -u root --exec /bin/true` 唤醒 → 健康探测失败则拉起 agent：`wsl -d <distro> -u root -- sh -c 'AGENT_PORT=<port> AGENT_AUTH_TOKEN=<token> setsid nohup node /opt/qm/agent.mjs >/var/log/qm-agent.log 2>&1 &'` → `/health` 轮询（30s）
- agent 端口：core 侧 `net.createServer` listen 0 取空闲端口（同 `test/local-sandbox.test.ts:25-33` 的 freePort 写法），注入 AGENT_PORT；HTTP 打 `http://127.0.0.1:<port>`（WSL2 localhost 转发），header 带 `x-agent-token`。agent.mjs 现状即 bind `0.0.0.0`，**保持不变**——bind `127.0.0.1` 会让 localhost 转发够不到它（经典踩坑点）
- `run`/`writeFileBytes`/`readFileBytes`/文件操作/进程会话/备份：全部对照 `local-sandbox.ts:335-494` 的同构实现（exec → `execRaw` 走 `/exec`，writeInline → `writeAbsBytes` 走 `/write`）
- `teardown`：`keepWarm` 与默认 → no-op（distro 常驻，WSL2 空闲自动回收）；`destroy` → `rm -rf <home>`（scope 工作区销毁，distro 不动）
- `profile`：`{ backend: "wsl2", writablePersistence: "resident_disk", processSessions: true, egressEnforcement: "domain", spec: { os: "Debian 12 (bookworm) — WSL2 guest", runtimes: ["Node 24", "Python 3"], tools: ["git","curl","wget","jq","unzip","python3","pip"], homeDir, workdir } }`
- 不实现 `stageIn`/`stageOut`/`extractFiles`/`reapDeepIdle`（对齐 local 先例）

- [ ] **Step 1: 写 fake-wsl 与失败测试**

```typescript
// test/support/fake-wsl.ts
import type { WslExec } from "../../src/sandbox/wsl-exec.ts";

export interface FakeWsl {
  wslExec: WslExec;
  distros: Set<string>;
  agentLaunched: { port: number; token: string } | null;
  wslDown: boolean;
}

export function installFakeWsl(): FakeWsl {
  const self: FakeWsl = {
    distros: new Set(["meerkat-sandbox"]),
    agentLaunched: null,
    wslDown: false,
    wslExec: async (args) => exec(args),
  };
  const ok = (stdout = "") => ({ code: 0, stdout, stderr: "" });
  const fail = (stderr: string) => ({ code: 1, stdout: "", stderr });
  function exec(args: string[]) {
    if (self.wslDown) return fail("wsl.exe not found");
    if (args[0] === "-l" && args[1] === "-q") return ok([...self.distros].join("\n") + "\n");
    if (args[0] === "-d") {
      const distro = args[1]!;
      if (!self.distros.has(distro)) return fail("There is no distribution with the supplied name.");
      const shIdx = args.indexOf("sh");
      if (shIdx > 0 && args[shIdx + 1] === "-c") {
        const script = args[shIdx + 2]!;
        const m = script.match(/AGENT_PORT=(\d+) AGENT_AUTH_TOKEN=(\S+)/);
        if (m) self.agentLaunched = { port: Number(m[1]), token: m[2]! };
      }
      return ok();
    }
    return fail(`fake wsl: unsupported args ${args.join(" ")}`);
  }
  return self;
}
```

测试骨架（daemon 起法同 `test/local-sandbox.test.ts:35-51`：spawn 真实 agent.mjs，env 带 `AGENT_AUTH_TOKEN`）：

```typescript
// test/wsl2-sandbox.test.ts
test("profile declares the wsl2 substrate honestly", () => {
  const sb = makeSandbox(installFakeWsl());
  assert.equal(sb.profile.backend, "wsl2");
  assert.equal(sb.profile.processSessions, true);
  assert.equal(sb.profile.egressEnforcement, "domain");
  assert.equal(supportsProcessSessions(sb), true);
});

test("missing distro fails provision with the import hint", async () => {
  const fake = installFakeWsl();
  fake.distros.clear();
  await assert.rejects(makeSandbox(fake).provision(rw("personal:U0")), /rootfs not imported/);
});

test("agent is launched with the per-boot token and every daemon call carries it", async () => {
  const fake = installFakeWsl();
  const sb = makeSandbox(fake);
  const h = await sb.provision(rw("personal:U0"));
  assert.equal(fake.agentLaunched?.token, AGENT_TOKEN);
  const r = await sb.run(h, "echo hello");
  assert.match(r.stdout, /hello/);
});

test("run executes in the per-scope workspace via the real agent", async () => {
  const sb = makeSandbox(installFakeWsl());
  const h = await sb.provision(rw("personal:U0"));
  await sb.writeFile(h, "a.txt", "content");
  assert.equal(await sb.readFile(h, "a.txt"), "content");
  assert.deepEqual(await sb.listDir(h, "."), ["a.txt"]);
});

test("destroy teardown removes the scope home; default teardown keeps the distro", async () => {
  const sb = makeSandbox(installFakeWsl());
  const h = await sb.provision(rw("personal:U0"));
  await sb.teardown(h);
  assert.equal((await sb.run(h, "true")).code, 0);
  await sb.teardown(h, { destroy: true });
  const gone = await sb.run(h, `test -d ${h.homeDir} && echo yes || echo no`);
  assert.match(gone.stdout, /no/);
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --experimental-test-module-mocks --test test/wsl2-sandbox.test.ts`
Expected: FAIL（模块不存在）

- [ ] **Step 3: 实现 `wsl-exec.ts`**

```typescript
// src/sandbox/wsl-exec.ts
import { execFile } from "node:child_process";

export type WslExec = (args: string[], timeoutMs?: number) => Promise<{ code: number; stdout: string; stderr: string }>;

export function spawnWslExec(wslBin = "wsl.exe"): WslExec {
  return (args, timeoutMs = 30_000) =>
    new Promise((resolve, reject) => {
      execFile(wslBin, args, { timeout: timeoutMs, maxBuffer: 16 * 1024 * 1024, windowsHide: true }, (err, stdout, stderr) => {
        if (err && typeof err.code !== "number") return reject(err);
        const code = typeof err?.code === "number" ? err.code : err ? 1 : 0;
        resolve({ code, stdout: String(stdout), stderr: String(stderr) });
      });
    });
}
```

注意：wsl.exe 在 Windows 上部分输出是 UTF-16——`wsl -l -q` 的 stdout 需按 UTF-16LE 容错解析。实现时给 `encoding: "buffer"`，对 `-l -q` 结果先尝 utf8 再尝 utf16le（出现 `\0` 即 utf16），提供 `decodeWslList(buf)` 辅助。

- [ ] **Step 4: 实现 `wsl2-sandbox.ts`**

骨架（对照 `local-sandbox.ts:91-497` 逐方法同构改写；daemon 层替换为直连固定端口）：

```typescript
// src/sandbox/wsl2-sandbox.ts（关键签名与主体；辅助层装配与 local 完全相同）
export interface Wsl2SandboxOptions {
  distro?: string;
  agentToken: string;
  wsl?: WslExec;
  fetchImpl?: typeof fetch;
  defaultTimeoutSec?: number;
  onError?: (e: { category: string; code: string; message: string; scopeLabel?: string }) => void;
}

export function createWsl2Sandbox(workspace: WorkspaceStore, opts: Wsl2SandboxOptions): Sandbox {
  const distro = opts.distro ?? "meerkat-sandbox";
  const wsl = opts.wsl ?? spawnWslExec();
  const fetchImpl = opts.fetchImpl ?? fetch;
  const token = opts.agentToken;
  let agentPort: number | undefined;

  async function ensureDistro(): Promise<void> {
    const list = await wsl(["-l", "-q"], 15_000);
    if (list.code !== 0) throw new Error("wsl2 sandbox: WSL2 is not enabled (enable it from Settings - Sandbox)");
    const names = decodeWslList(list.stdout).split("\n").map((s) => s.trim()).filter(Boolean);
    if (!names.includes(distro)) {
      throw new Error(`wsl2 sandbox: rootfs not imported (distro ${distro} missing) - it is imported at app startup; check the boot checklist`);
    }
    const wake = await wsl(["-d", distro, "-u", "root", "--exec", "/bin/true"], 30_000);
    if (wake.code !== 0) throw new Error(`wsl2 sandbox: distro ${distro} failed to start: ${wake.stderr.trim()}`);
  }

  async function daemon(path: string, body?: unknown, timeoutMs?: number) {
    if (!agentPort) throw new Error("wsl2 sandbox: agent port not assigned");
    const res = await fetchImpl(`http://127.0.0.1:${agentPort}${path}`, {
      method: body === undefined ? "GET" : "POST",
      headers: { "x-agent-token": token, ...(body ? { "content-type": "application/json" } : {}) },
      ...(body ? { body: JSON.stringify(body) } : {}),
      signal: AbortSignal.timeout(timeoutMs ?? 30_000),
    });
    if (res.status === 401) throw new Error("wsl2 sandbox: agent rejected the per-boot token (stale agent? restart the app)");
    return { status: res.status, text: await res.text() };
  }

  async function ensureAgent(): Promise<void> {
    await ensureDistro();
    if (agentPort) {
      try { if ((await daemon("/health", undefined, 3000)).status === 200) return; } catch { /* relaunch */ }
    }
    agentPort = await freePort();
    const launch = await wsl(["-d", distro, "-u", "root", "--", "sh", "-c",
      `AGENT_PORT=${agentPort} AGENT_AUTH_TOKEN=${token} setsid nohup node /opt/qm/agent.mjs >/var/log/qm-agent.log 2>&1 &`], 15_000);
    if (launch.code !== 0) throw new Error(`wsl2 sandbox: agent launch failed: ${launch.stderr.trim()}`);
    // /health 轮询 30s（同 local-sandbox waitDaemon:178-192）
  }
  // execRaw/writeAbsBytes/readAbsBytes 对照 local-sandbox.ts:207-226，走 daemon()
  // provision/run/teardown/文件/进程/备份：对照 local-sandbox.ts:335-494
  // 差异仅：home = `/home/qm-${slug(scope)}`、无容器/网络/卷概念、teardown 语义如上
}
```

`src/sandbox/sandbox-routing.ts:17` 加 `"wsl2"`。

- [ ] **Step 5: 跑测试确认通过 + typecheck**

Run: `node --experimental-test-module-mocks --test test/wsl2-sandbox.test.ts test/sandbox-routing.test.ts test/none-sandbox.test.ts`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add src/sandbox/wsl-exec.ts src/sandbox/wsl2-sandbox.ts src/sandbox/sandbox-routing.ts test/support/fake-wsl.ts test/wsl2-sandbox.test.ts
git commit -m "feat(core): wsl2 sandbox backend over the microvm agent protocol"
```

### Task 4: wiring `buildWsl2` + config `wsl2SandboxEnv`

**Files:**
- Modify: `src/config.ts`（`Config` 接口加 `wsl2Sandbox`、`sandboxBackend` 联合加 `"wsl2"`、`sandboxBackendEnvStrict` 接受、新增 `wsl2SandboxEnv(env)`、config 组装处挂入）
- Modify: `src/wiring.ts:607-611`（`buildBackend` 加 `wsl2` 分支）
- Test: `test/config.test.ts`

**Interfaces:**
- Consumes: `createWsl2Sandbox`（Task 3）
- Produces: env 契约——`WSL2_SANDBOX_DISTRO`（默认 `meerkat-sandbox`）；`SANDBOX_BACKEND=wsl2` 时 wiring 以 `randomBytes(32)` 生成 per-boot `agentToken` 传给 backend

- [ ] **Step 1: 写失败测试**

```typescript
test("WSL2_SANDBOX_DISTRO overrides the default distro", () => {
  assert.equal(loadConfigFromEnv({ ...baseEnv, SANDBOX_BACKEND: "wsl2", WSL2_SANDBOX_DISTRO: "custom" }).wsl2Sandbox.distro, "custom");
});
test("wsl2Sandbox defaults to meerkat-sandbox", () => {
  assert.equal(loadConfigFromEnv({ ...baseEnv, SANDBOX_BACKEND: "wsl2" }).wsl2Sandbox.distro, "meerkat-sandbox");
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --experimental-test-module-mocks --test test/config.test.ts`
Expected: FAIL（`wsl2Sandbox` undefined）

- [ ] **Step 3: 实现**

config.ts 加（对照 `localSandboxEnv`，`src/config.ts:258-267`）：

```typescript
function wsl2SandboxEnv(env: NodeJS.ProcessEnv): { distro: string } {
  const d = env.WSL2_SANDBOX_DISTRO?.trim();
  return { distro: d || "meerkat-sandbox" };
}
```

`Config` 接口加 `wsl2Sandbox: { distro: string }`；组装处加 `wsl2Sandbox: wsl2SandboxEnv(env)`；`sandboxBackend` 联合类型与 `sandboxBackendEnvStrict` 加 `"wsl2"`（报错文案 `aws, local, sprites, wsl2, or none`——Task 1 的 config 用例已按此文案写）。production 必填校验（`config.ts:615-617`）文案同步。

wiring.ts 加：

```typescript
const buildWsl2 = (): Sandbox =>
  createWsl2Sandbox(workspace, {
    ...config.wsl2Sandbox,
    agentToken: randomBytes(32).toString("hex"),
    onError: sandboxOnError,
  });
const buildBackend: Record<Config["sandboxBackend"], () => Sandbox> = {
  local: buildLocal, sprites: buildSprites, aws: buildAws, none: () => createNoneSandbox(), wsl2: buildWsl2,
};
```

（`randomBytes` 从 `node:crypto` import；`createNoneSandbox`/`createWsl2Sandbox` import。）

- [ ] **Step 4: 跑测试确认通过**

Run: `node --experimental-test-module-mocks --test test/config.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/config.ts src/wiring.ts test/config.test.ts
git commit -m "feat(core): wire wsl2 backend with per-boot agent token"
```

---

## Phase 2: rootfs 构建与分发

### Task 5: rootfs Dockerfile + 构建脚本 + 指纹

**Files:**
- Create: `deploy/layers/meerkat/desktop/rootfs/Dockerfile`
- Create: `deploy/layers/meerkat/desktop/scripts/build-rootfs.sh`
- Test: 手动验证（构建产物导入真机 WSL2 冒烟，Step 4）

**Interfaces:**
- Produces: `deploy/layers/meerkat/desktop/payload-sandbox/rootfs.tar.gz` 与 `payload-sandbox/fingerprint.txt`（Task 6 拷入 payload；Task 7 的 Tauri 侧读取 fingerprint 做更新判定）
- Consumes: `computeSandboxImageFingerprint`（`src/sandbox/local-sandbox.ts:56-75`）——指纹源含 `aws/microvm-agent/agent.mjs` 与 `fly/tools/*`，rootfs 与 local 镜像共用同一指纹，agent 改动天然触发重导

**设计要点**（设计文档第三节）：Docker 仅开发机/CI 构建依赖，客户机不需要。rootfs 内容：Debian bookworm-slim + Node 24（仅 `node` 二进制）+ python3/pip/pandas + git/curl/wget/jq/unzip + iptables + `/opt/qm/agent.mjs` + 指纹文件。目标 tar.gz ≤110MB。

- [ ] **Step 1: 写 rootfs Dockerfile**

```dockerfile
# deploy/layers/meerkat/desktop/rootfs/Dockerfile
FROM debian:bookworm-slim
ARG NODE_VERSION=v24.15.0
RUN apt-get update && apt-get install -y --no-install-recommends \
      ca-certificates git curl wget jq unzip xz-utils \
      python3 python3-pip python3-venv iptables \
    && pip3 install --break-system-packages pandas \
    && apt-get clean && rm -rf /var/lib/apt/lists/* /usr/share/doc /usr/share/man
RUN curl -fsSL "https://nodejs.org/dist/${NODE_VERSION}/node-${NODE_VERSION}-linux-x64.tar.xz" \
      | tar -xJ -C /usr/local --strip-components=1 "node-${NODE_VERSION}-linux-x64/bin/node"
RUN mkdir -p /opt/qm
COPY aws/microvm-agent/agent.mjs /opt/qm/agent.mjs
ARG FINGERPRINT=dev
RUN echo "${FINGERPRINT}" > /etc/qm-sandbox-fingerprint
```

- [ ] **Step 2: 写构建脚本**

```bash
#!/usr/bin/env bash
# deploy/layers/meerkat/desktop/scripts/build-rootfs.sh
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/../../../../.." && pwd)"
DESKTOP="$ROOT/deploy/layers/meerkat/desktop"
OUT="$DESKTOP/payload-sandbox"
mkdir -p "$OUT"

FINGERPRINT="$(node -e 'import("./src/sandbox/local-sandbox.ts").then(m => m.computeSandboxImageFingerprint(process.cwd())).then(f => console.log(f ?? "dev"))')"
echo "fingerprint: $FINGERPRINT"

docker build --platform linux/amd64 --build-arg "FINGERPRINT=$FINGERPRINT" \
  -f "$DESKTOP/rootfs/Dockerfile" -t meerkat-sandbox-rootfs "$ROOT"
cid="$(docker create meerkat-sandbox-rootfs)"
docker export "$cid" | gzip -9 > "$OUT/rootfs.tar.gz"
docker rm "$cid" >/dev/null
printf '%s' "$FINGERPRINT" > "$OUT/fingerprint.txt"
ls -lh "$OUT/rootfs.tar.gz"
```

注：`docker export` 产出的是完整根文件系统 tar，`wsl --import` 直接吃；`--platform linux/amd64` 保证 Windows x64 客户机可跑（arm64 Windows 不在 v0.1.1 支持面）。

- [ ] **Step 3: 构建并核对体积**

Run: `bash deploy/layers/meerkat/desktop/scripts/build-rootfs.sh`
Expected: `rootfs.tar.gz` ≤110MB；超标则排查大头（`docker run --rm meerkat-sandbox-rootfs du -sh /usr /opt /root`），常见剩余：pip 缓存（加 `--no-cache-dir`）、apt 列表、locale。

- [ ] **Step 4: 真机冒烟**

```powershell
wsl --import meerkat-sandbox-test "$env:TEMP\wsl-test" deploy\layers\meerkat\desktop\payload-sandbox\rootfs.tar.gz
wsl -d meerkat-sandbox-test -u root -- node /opt/qm/agent.mjs --help 2>$null; wsl -d meerkat-sandbox-test -u root -- cat /etc/qm-sandbox-fingerprint
wsl -d meerkat-sandbox-test -u root -- sh -c "python3 -c 'import pandas; print(pandas.__version__)'"
wsl --unregister meerkat-sandbox-test
```

Expected: 指纹输出与 `fingerprint.txt` 一致；pandas 可用。

- [ ] **Step 5: Commit**

```bash
git add deploy/layers/meerkat/desktop/rootfs/Dockerfile deploy/layers/meerkat/desktop/scripts/build-rootfs.sh
git commit -m "feat(desktop): rootfs build script for the wsl2 sandbox"
```

（`payload-sandbox/` 是构建产物，加入 `deploy/layers/meerkat/desktop/.gitignore`。）

### Task 6: stage-payload 集成 rootfs

**Files:**
- Modify: `deploy/layers/meerkat/desktop/scripts/stage-payload.sh`、`stage-payload.ps1`
- Modify: `deploy/layers/meerkat/desktop/scripts/verify-payload.sh`

**Interfaces:**
- Consumes: Task 5 的 `payload-sandbox/` 产物
- Produces: payload 布局新增 `payload/sandbox/rootfs.tar.gz` + `payload/sandbox/fingerprint.txt`（Task 7 的 Tauri `StackCtx.payload_dir` 直下可读）

- [ ] **Step 1: 改 stage-payload.sh（ps1 同步）**

在拷种子（`stage-payload.sh:71`）之后追加：

```bash
if [ -f "$DESKTOP/payload-sandbox/rootfs.tar.gz" ]; then
  mkdir -p "$PAYLOAD/sandbox"
  cp "$DESKTOP/payload-sandbox/rootfs.tar.gz" "$DESKTOP/payload-sandbox/fingerprint.txt" "$PAYLOAD/sandbox/"
else
  echo "warn: payload-sandbox/rootfs.tar.gz missing — run scripts/build-rootfs.sh first (Windows sandbox will ship disabled)"
fi
```

ps1 版同逻辑。缺 rootfs 只警告不失败——macOS 构建本就不带沙箱（第十一节平台矩阵：mac 包不放 `payload/sandbox/`，proc.rs 注入 `none`）。

- [ ] **Step 2: verify-payload.sh 加体积与存在性核对**

```bash
du -sh "$PAYLOAD/sandbox" 2>/dev/null || true
[ -f "$PAYLOAD/sandbox/rootfs.tar.gz" ] && echo "sandbox rootfs staged" || echo "sandbox rootfs absent (mac build or skipped)"
```

- [ ] **Step 3: 全量 staging 验证**

Run: `bash deploy/layers/meerkat/desktop/scripts/stage-payload.sh`
Expected: `payload/sandbox/` 存在；`du -sh payload` 总量仍在 <200MB 压缩后目标的对应量级（payload 未压缩口径 ≈148MB + rootfs ~110MB）。

- [ ] **Step 4: Commit**

```bash
git add deploy/layers/meerkat/desktop/scripts/stage-payload.sh deploy/layers/meerkat/desktop/scripts/stage-payload.ps1 deploy/layers/meerkat/desktop/scripts/verify-payload.sh deploy/layers/meerkat/desktop/.gitignore
git commit -m "feat(desktop): stage sandbox rootfs into the payload"
```

---

## Phase 3: Tauri 壳

### Task 7: `sandbox.rs`——检测 / 启用 / 导入 + 命令注册 + ACL

**Files:**
- Create: `deploy/layers/meerkat/desktop/src-tauri/src/sandbox.rs`
- Modify: `deploy/layers/meerkat/desktop/src-tauri/src/main.rs`（`mod sandbox;` + 两个 command 注册）
- Modify: `deploy/layers/meerkat/desktop/src-tauri/build.rs`（commands 清单加两项）
- Modify: `deploy/layers/meerkat/desktop/src-tauri/capabilities/default.json`、`local.json`
- Modify: `deploy/layers/meerkat/desktop/src-tauri/Cargo.toml`（windows crate 加 Win32_UI_Shell / Win32_System_Registry feature）

**Interfaces:**
- Produces（前端契约，Task 8/9 消费）：
  - `wsl2_status() -> SandboxStatus`，`SandboxStatus = { supported: bool, os_build: u32, wsl_enabled: bool, imported: bool, fingerprint_current: bool, pending_reboot: bool, reason: String | null }`
  - `enable_wsl2() -> Result<(), String>`（ShellExecute runas 提权 `wsl --install --no-distribution`，随后置 `pending_reboot`）
  - `ensure_rootfs(app) -> Result<SandboxStatus, String>`（非 command，启动流程调用：未导入→`wsl --import`；指纹不匹配→重导；全程 emit `component-ready`/`component-failed`（component `"sandbox"`，severity 恒 `degraded`））
- 状态文件 `DATA_DIR/sandbox-state.json`：`{ imported: bool, fingerprint: string, pending_reboot: bool }`

- [ ] **Step 1: 实现 sandbox.rs 状态检测**

```rust
// deploy/layers/meerkat/desktop/src-tauri/src/sandbox.rs
use serde::Serialize;
use std::fs;
use std::path::{Path, PathBuf};
use std::process::{Command, Stdio};

pub const DISTRO: &str = "meerkat-sandbox";
pub const MIN_OS_BUILD: u32 = 19044;   // Win10 21H2；wsl --install --no-distribution 完整可用的底线
                                       // （19041 名义支持 wsl --install 但 --no-distribution 等标志更晚才齐；
                                       //  若 Task 0 spike 验证 19041-19043 实际可用，再下调并加 dism 回退分支）

#[derive(Clone, Serialize)]
#[serde(rename_all = "snake_case")]
pub struct SandboxStatus {
    pub supported: bool,
    pub os_build: u32,
    pub wsl_enabled: bool,
    pub imported: bool,
    pub fingerprint_current: bool,
    pub pending_reboot: bool,
    pub reason: Option<String>,
}

#[derive(serde::Deserialize, serde::Serialize, Default)]
struct StateFile {
    #[serde(default)] imported: bool,
    #[serde(default)] fingerprint: String,
    #[serde(default)] pending_reboot: bool,
}

fn state_path(data_dir: &Path) -> PathBuf { data_dir.join("sandbox-state.json") }

fn load_state(data_dir: &Path) -> StateFile {
    fs::read_to_string(state_path(data_dir)).ok()
        .and_then(|t| serde_json::from_str(&t).ok()).unwrap_or_default()
}

fn save_state(data_dir: &Path, s: &StateFile) -> std::io::Result<()> {
    fs::write(state_path(data_dir), serde_json::to_string_pretty(s).unwrap())
}

#[cfg(windows)]
fn os_build() -> u32 {
    use windows::core::w;
    use windows::Win32::System::Registry::{RegGetValueW, HKEY_LOCAL_MACHINE, RRF_RT_REG_SZ};
    let mut buf = [0u16; 16];
    let mut len = (buf.len() * 2) as u32;
    let ok = unsafe {
        RegGetValueW(
            HKEY_LOCAL_MACHINE,
            w!("SOFTWARE\\Microsoft\\Windows NT\\CurrentVersion"),
            w!("CurrentBuildNumber"),
            RRF_RT_REG_SZ,
            None,
            Some(buf.as_mut_ptr() as *mut _),
            Some(&mut len),
        )
    };
    if ok.is_err() { return 0; }
    let end = buf.iter().position(|c| *c == 0).unwrap_or(buf.len());
    String::from_utf16_lossy(&buf[..end]).trim().parse().unwrap_or(0)
}

#[cfg(not(windows))]
fn os_build() -> u32 { 0 }

fn wsl(args: &[&str]) -> std::io::Result<std::process::Output> {
    let mut cmd = Command::new("wsl.exe");
    cmd.args(args);
    #[cfg(windows)]
    {
        use std::os::windows::process::CommandExt;
        cmd.creation_flags(0x08000000);
    }
    cmd.output()
}

fn decode_wsl_out(bytes: &[u8]) -> String {
    if bytes.iter().any(|b| *b == 0) {
        String::from_utf16_lossy(&bytes.chunks_exact(2).map(|c| u16::from_le_bytes([c[0], c[1]])).collect::<Vec<_>>())
    } else {
        String::from_utf8_lossy(bytes).to_string()
    }
}

pub fn status(data_dir: &Path, payload_dir: &Path) -> SandboxStatus {
    let build = os_build();
    let st = load_state(data_dir);
    let wsl_enabled = wsl(&["--status"]).map(|o| o.status.success()).unwrap_or(false);
    let imported = wsl(&["-l", "-q"]).map(|o| decode_wsl_out(&o.stdout).lines().any(|l| l.trim() == DISTRO)).unwrap_or(false);
    let want_fp = fs::read_to_string(payload_dir.join("sandbox").join("fingerprint.txt")).unwrap_or_default();
    let fingerprint_current = imported && !want_fp.is_empty() && st.fingerprint == want_fp.trim();
    SandboxStatus {
        supported: build >= MIN_OS_BUILD,
        os_build: build,
        wsl_enabled,
        imported,
        fingerprint_current,
        pending_reboot: st.pending_reboot,
        reason: if build < MIN_OS_BUILD { Some(format!("OS build {build} 低于沙箱要求的 {MIN_OS_BUILD}")) } else { None },
    }
}
```

- [ ] **Step 2: 实现 enable_wsl2 与 ensure_rootfs**

```rust
#[cfg(windows)]
pub fn enable_wsl2(data_dir: &Path) -> Result<(), String> {
    use windows::core::w;
    use windows::Win32::UI::Shell::ShellExecuteW;
    use windows::Win32::UI::WindowsAndMessaging::SW_SHOWNORMAL;
    // runas 动词触发一次 UAC；--no-distribution 只装平台不装 Ubuntu
    let r = unsafe {
        ShellExecuteW(None, w!("runas"), w!("wsl.exe"), w!("--install --no-distribution"), None, SW_SHOWNORMAL)
    };
    if (r.0 as usize) <= 32 { return Err("用户取消了授权或启用失败".into()); }
    let mut st = load_state(data_dir);
    st.pending_reboot = true;
    save_state(data_dir, &st).map_err(|e| e.to_string())
}

pub fn ensure_rootfs(app: &tauri::AppHandle, payload_dir: &Path, data_dir: &Path) -> Result<(), String> {
    let st = status(data_dir, payload_dir);
    if !st.supported || !st.wsl_enabled { return Ok(()); }   // 降级：none，无需动作
    let tar = payload_dir.join("sandbox").join("rootfs.tar.gz");
    if !tar.exists() { return Ok(()); }                       // mac 构建无 rootfs
    let need_import = !st.imported || !st.fingerprint_current;
    if !need_import { return Ok(()); }
    let phase = if st.imported { "更新中" } else { "初始化中" };
    emit_sandbox(app, "pending", phase);
    let backup = data_dir.join("wsl-home-backup.tar");
    if st.imported {
        // 数据保护（必修）：重导前把 guest /home 导出到 DATA_DIR 暂存，导入后恢复——
        // wsl --unregister 会销毁整个 vhdx，所有 scope 的 workspace 文件全丢，
        // 指纹源含 agent.mjs 与 fly/tools，活跃发版期每次升级都可能触发，不能静默清数据。
        // wsl.exe 自身输出才有 UTF-16 问题，guest 命令的 stdout 透传是字节干净的，可直接接文件。
        let out = Command::new("wsl.exe")
            .args(["-d", DISTRO, "-u", "root", "--", "tar", "-cf", "-", "-C", "/", "home"])
            .stdout(Stdio::from(fs::File::create(&backup).map_err(|e| e.to_string())?))
            .output().map_err(|e| e.to_string())?;
        if !out.status.success() {
            emit_sandbox(app, "failed", "workspace 备份失败，已中止更新（旧沙箱保留）");
            return Err("rootfs update aborted: /home backup failed".into());   // 绝不无备份 unregister
        }
        let _ = wsl(&["--unregister", DISTRO]);
    }
    let install_dir = data_dir.join("wsl");
    fs::create_dir_all(&install_dir).map_err(|e| e.to_string())?;
    let out = wsl(&["--import", DISTRO, &install_dir.to_string_lossy(), &tar.to_string_lossy()])
        .map_err(|e| e.to_string())?;
    if !out.status.success() {
        emit_sandbox(app, "failed", &decode_wsl_out(&out.stderr));
        return Err(format!("rootfs 导入失败: {}", decode_wsl_out(&out.stderr)));
    }
    if backup.exists() {
        let restore = Command::new("wsl.exe")
            .args(["-d", DISTRO, "-u", "root", "--", "tar", "-xf", "-", "-C", "/"])
            .stdin(Stdio::from(fs::File::open(&backup).map_err(|e| e.to_string())?))
            .output().map_err(|e| e.to_string())?;
        if !restore.status.success() {
            emit_sandbox(app, "failed", "workspace 恢复失败，备份保留在 DATA_DIR/wsl-home-backup.tar");
            return Err("rootfs updated but /home restore failed (backup retained)".into());
        }
        let _ = fs::remove_file(&backup);
    }
    let fp = fs::read_to_string(payload_dir.join("sandbox").join("fingerprint.txt")).unwrap_or_default();
    save_state(data_dir, &StateFile { imported: true, fingerprint: fp.trim().into(), pending_reboot: false }).map_err(|e| e.to_string())?;
    emit_sandbox(app, "ready", "");
    Ok(())
}
```

（`emit_sandbox` 复用 proc.rs 的 `component-ready`/`component-failed` 事件形状，component 固定 `"sandbox"`，severity 恒 `degraded`。）

- [ ] **Step 3: 注册命令与 ACL**

main.rs：`mod sandbox;`；`invoke_handler` 加 `wsl2_status`、`enable_wsl2` 两个 command（`wsl2_status` 从 `Boot` state 取 payload/data dir 调 `sandbox::status`）。

build.rs 的 `commands(&[...])` 加 `"wsl2_status"`、`"enable_wsl2"`（tauri-build 自动生成 `allow-wsl2-status`、`allow-enable-wsl2`）。

capabilities：`default.json`（web-ui 远程来源）permissions 加 `allow-wsl2-status`、`allow-enable-wsl2`（设置页要用）；`local.json` 同步加。

- [ ] **Step 4: 编译验证**

Run: `cd deploy/layers/meerkat/desktop/src-tauri && ../../../../../../deploy/layers/meerkat/desktop/scripts/cargo-msvc.bat cargo check`（或开发机上惯用的 cargo check 方式）
Expected: 编译通过；`gen/schemas/capabilities.json` 重新生成含两个新权限。

- [ ] **Step 5: Commit**

```bash
git add deploy/layers/meerkat/desktop/src-tauri
git commit -m "feat(desktop): sandbox module — wsl2 status, uac enablement, rootfs import with fingerprint"
```

### Task 8: proc.rs 平台映射 + 启动序列集成 + splash 沙箱行

**Files:**
- Modify: `deploy/layers/meerkat/desktop/src-tauri/src/proc.rs`（`spawn_component` Core 分支的 `SANDBOX_BACKEND` 注入、启动序列）
- Modify: `deploy/layers/meerkat/desktop/src-tauri/src/main.rs`（setup 里调 `ensure_rootfs`）
- Modify: `deploy/layers/meerkat/desktop/ui/index.html`（清单加 `item-sandbox` 行）

**Interfaces:**
- Consumes: Task 7 的 `sandbox::status` / `sandbox::ensure_rootfs`
- Produces: 平台映射表（设计文档第一节）——`Windows + imported && fingerprint_current → "wsl2"`；其余 → `"none"`；macOS 恒 `"none"`

- [ ] **Step 1: proc.rs 注入改造**

`StackCtx` 加 `sandbox_backend: &'static str`（main.rs setup 在 `ensure_rootfs` 之后计算：`status().fingerprint_current ? "wsl2" : "none"`，非 Windows 恒 `"none"`）。`spawn_component` Core 分支把 `.env("SANDBOX_BACKEND", "local")`（`proc.rs:243`）改为 `.env("SANDBOX_BACKEND", ctx.sandbox_backend)`。

- [ ] **Step 2: main.rs 启动序列集成（顺序必修：窗口先行）**

现状 `main.rs` 的 setup 闭包里窗口是**最后**才建的（`WebviewWindowBuilder` 在 `spawn_stack` 之后）。若 `ensure_rootfs` 在 setup 线程同步跑，导入的 ~30s 里 splash 根本不存在——用户看到的是双击图标后 30 秒无窗口假死。因此 setup 顺序必须调整为：

```text
1. 建窗口（WebviewWindowBuilder 提到最前，splash 立即可见）
2. sandbox::status() 初始探测 → 结果存 managed state（splash 打开时 invoke("wsl2_status") 即得）
3. sandbox::ensure_rootfs() 同步执行（~30s；期间 splash 沙箱行黄灯"初始化中/更新中"）
4. 计算 sandbox_backend（status().fingerprint_current ? "wsl2" : "none"，非 Windows 恒 "none"）
5. spawn_stack → health_poll
```

两个配套说明：

- **事件错过问题**：`app.emit` 的事件在窗口 JS 加载完成前发出会丢——splash 侧用与现有 `syncStatus()` 相同的模式兜底：打开时 `invoke("wsl2_status")` 拿初始状态，之后每 2s 轮询直到 `imported && fingerprint_current` 转绿。emit 照发（加载完成后的事件仍有价值），但正确性不依赖它
- `ensure_rootfs` 返回 `Err` 不阻断启动：记状态后按 `none` 继续（降级纪律）
- 调整顺序后 `fatal()` 路径（spawn 失败）同样在窗口之后执行，行为不变；异步导入留待 v0.1.x 优化

- [ ] **Step 3: splash 加沙箱行**

`ui/index.html` 三处改动（对照现有三行模式，107-127 行）：

```html
<div class="item pending" id="item-sandbox">
  <div class="dot"></div>
  <div class="body">
    <div class="row"><span class="name">沙箱</span><span class="meta"></span></div>
    <div class="reason"></div>
  </div>
</div>
```

JS：`state` 加 `sandbox: "pending"`（143 行）；事件监听已按 component 名分发，无需改；`maybeEnter()`（168-177 行）**不等待** sandbox 行——黄灯可进主界面（降级纪律）；失败展示沿用 `.reason`。

初始状态细化：打开 splash 时先 `invoke("wsl2_status")`——`supported=false` → 行显示"此机器不支持沙箱"（degraded）；`!wsl_enabled` → "未启用（可在设置页开启）"（degraded）；否则 pending。pending 期间每 2s 轮询 `wsl2_status`（与 `syncStatus()` 同模式），`imported && fingerprint_current` 转 true → 转绿——这条轮询是正确性主路径，覆盖窗口 JS 加载完成前 emit 丢失的情况。

- [ ] **Step 4: 编译 + 手工跑 dev 实例看清单**

Expected: 未启用 WSL2 的机器上沙箱行显示降级文案且不阻断进主界面。

- [ ] **Step 5: Commit**

```bash
git add deploy/layers/meerkat/desktop/src-tauri/src deploy/layers/meerkat/desktop/ui/index.html
git commit -m "feat(desktop): platform-mapped SANDBOX_BACKEND + sandbox row in the boot checklist"
```

### Task 9: 设置页「沙箱」区块

**Files:**
- Modify: `plugins/web-ui/server/setup.html`（新增 section + JS）
- Test: 手动（Task 16 真机验收覆盖）

**Interfaces:**
- Consumes: Task 7 的 `wsl2_status` / `enable_wsl2` Tauri commands（ACL 已在 Task 7 放行 web-ui 来源）

- [ ] **Step 1: setup.html 加区块**

在「网络代理」section（`setup.html:91-95`）之后插入，沿用现有 `<section><label>…<p class="hint">` 模式：

```html
<section>
  <label>沙箱（代码执行能力）</label>
  <div id="sandbox-status">检测中…</div>
  <button type="button" id="sandbox-enable" hidden>启用沙箱（需要管理员授权）</button>
  <p class="hint">启用后 Meerkat 可以运行脚本与生成文件；未启用时仅对话能力可用。macOS 版本即将支持。</p>
</section>
```

JS（页面加载时）：

```javascript
async function refreshSandbox() {
  const el = document.getElementById("sandbox-status");
  const btn = document.getElementById("sandbox-enable");
  if (!window.__TAURI__) { el.textContent = "当前环境不支持沙箱"; return; }
  const s = await window.__TAURI__.core.invoke("wsl2_status");
  if (!s.supported) { el.textContent = "此机器不支持沙箱（" + (s.reason ?? "系统版本过低") + "）"; }
  else if (s.imported && s.fingerprint_current) { el.textContent = "✅ 沙箱已就绪"; }
  else if (s.pending_reboot) { el.textContent = "已启用，重启电脑后生效"; }
  else if (!s.wsl_enabled) { el.textContent = "未启用"; btn.hidden = false; }
  else { el.textContent = "已启用，将在下次启动时完成初始化"; }
}
btn.onclick = async () => {
  try { await window.__TAURI__.core.invoke("enable_wsl2"); await refreshSandbox(); }
  catch (e) { el.textContent = "启用失败：" + e; }
};
```

- [ ] **Step 2: 设置页常态可达性确认**

现状 `/setup` 仅在 `needsSetup()` 时强制进入（`plugins/web-ui/server/index.ts:2272-2275`）。沙箱区块需要"事后可回"——确认设置页在已完成首启后仍可手动访问（若不可，本 Task 附带放开：`/setup` 路由对已配置用户改为可访问不强制重定向）。此改动与优化三（Task 13）入口合并验证。

- [ ] **Step 3: Commit**

```bash
git add plugins/web-ui/server/setup.html plugins/web-ui/server/index.ts
git commit -m "feat(web-ui): sandbox section on the setup page with UAC enablement"
```

---

## Phase 4: 网络围栏（spike 已定论：guest 内围栏）

> Task 0 spike（2026-08-17，Win10 22H2 + WSL2 真机，五项用例全绿）已证明：围栏全部做在 guest 内部即可，宿主机防火墙不需要。结论与实测记录见 `spike-win10-egress.md`。本 Phase 按 spike 结论形状实现。

### Task 10: guest 内 CONNECT 代理 + iptables（owner 匹配）

**Files:**
- Create: `deploy/layers/meerkat/desktop/rootfs/egress-proxy.mjs`（guest 内代理，纯 Node 零依赖，仿 agent.mjs 形态）
- Create: `deploy/layers/meerkat/desktop/rootfs/egress-lock.sh`（spike 验证过的规则集）
- Create: `deploy/layers/meerkat/desktop/seeds/sandbox-egress.json`（内置白名单种子）
- Modify: `src/sandbox/wsl2-sandbox.ts`（provision 注入代理 env；ensureAgent 成功后拉起代理 + 应用 iptables）
- Modify: `src/wiring.ts`（`buildWsl2` 读种子、把白名单传入 backend）
- Modify: `deploy/layers/meerkat/desktop/rootfs/Dockerfile`（COPY 两个文件到 /opt/qm/）
- Test: `test/egress-proxy.test.ts`

**Interfaces:**
- Produces:
  - guest 代理契约：`/opt/qm/egress-proxy.mjs`，env `EGRESS_PORT` + `EGRESS_ALLOW`（逗号分隔域名）。CONNECT 仅放行白名单（精确 + 后缀匹配），其余 403；**只接 CONNECT（仅 https），明文 http 一律 403**——有意边界
  - `wsl2Sandbox` opts 新增 `egress?: { allowlist: string[] }`——存在则：provision 的 `handle.env` 注入 `HTTP_PROXY/HTTPS_PROXY=http://127.0.0.1:<EGRESS_PORT>`（`sandbox-env.ts` 的 `proxyExportPrefix` 在 run 时自动 export）；ensureAgent 成功后：写白名单到 guest `/etc/qm-egress-allow`（/write）→ 拉起代理（`EGRESS_PORT`/`EGRESS_ALLOW` env）→ exec 一次 `/opt/qm/egress-lock.sh`
  - `egress-lock.sh` 规则集（spike 实测）：`OUTPUT 默认 DROP` + 放行 lo + `ESTABLISHED,RELATED` + `-m owner --uid-owner 0 -j ACCEPT`（root 全权出网，仅覆盖 agent 与代理两个可信进程）
- 白名单种子格式：`{ "allow": ["pypi.org", "files.pythonhosted.org", "registry.npmjs.org", "github.com", "objects.githubusercontent.com", "mirrors.aliyun.com", "registry.npmmirror.com"] }`
- Consumes: `sandbox-env.ts` 的 `proxyExportPrefix`（`src/sandbox/sandbox-env.ts:30`）；Task 3 的 backend

**设计要点**（spike 结论，设计文档第五节）：
- 代理在 guest 内（root、loopback）——消灭原方案三个未决问题：guest→宿主网关可达性、Windows 防火墙入站弹窗、代理 bind 0.0.0.0 的暴露面
- root 出网放行只覆盖 agent 与代理；用户脚本经 Task 11 降权后为非 root，绕过代理的直连被 DROP（spike 用例 3 已验证）
- guest 内核缺 iptables（极老 WSL 内核）→ 记 onError 且 provision 报"沙箱无出网环境异常"——fail-closed，**不静默放行**

- [ ] **Step 1: 写失败测试**

```typescript
import { test, before, after } from "node:test";
import assert from "node:assert/strict";
import { spawn, type ChildProcess } from "node:child_process";
import { createServer, type AddressInfo } from "node:net";
import { connect } from "node:net";
import { join } from "node:path";

let daemon: ChildProcess;
let port = 0;

before(async () => {
  port = await freePort();
  daemon = spawn(process.execPath, [join(process.cwd(), "deploy/layers/meerkat/desktop/rootfs/egress-proxy.mjs")], {
    env: { ...process.env, EGRESS_PORT: String(port), EGRESS_ALLOW: "pypi.org,files.pythonhosted.org" },
    stdio: "ignore",
  });
  await waitListen(port);
});

after(() => { daemon?.kill("SIGKILL"); });

test("CONNECT to an allowlisted domain is tunneled", async () => {
  const upstream = createServer((req, res) => { res.writeHead(200); res.end("ok"); });
  await new Promise<void>((r) => upstream.listen(0, "127.0.0.1", r));
  const upstreamPort = (upstream.address() as AddressInfo).port;
  const res = await connectTo(port, "pypi.org", upstreamPort);
  assert.equal(res.statusCode, 200);
  upstream.close();
});

test("CONNECT to a non-allowlisted domain gets 403", async () => {
  const res = await connectTo(port, "evil.com", 443);
  assert.equal(res.statusCode, 403);
});

test("plain http (non-CONNECT) gets 403", async () => {
  const res = await rawGet(port, "http://pypi.org/simple/");
  assert.equal(res.statusCode, 403);
});
```

（`freePort`/`waitListen`/`connectTo`/`rawGet` 辅助沿用 test/agent-auth.test.ts 的形状。）

- [ ] **Step 2: 跑测试确认失败**

Run: `node --experimental-test-module-mocks --test test/egress-proxy.test.ts`
Expected: FAIL（文件不存在）

- [ ] **Step 3: 实现 egress-proxy.mjs + egress-lock.sh**

egress-proxy.mjs：Node `http.createServer` + `server.on("connect")`，CONNECT 按 `EGRESS_ALLOW` 精确 + 后缀匹配，拒绝 403；request 处理器一律 403（CONNECT-only）。零依赖、无注释，形态对照 `aws/microvm-agent/agent.mjs`。

egress-lock.sh：

```sh
#!/bin/sh
set -e
iptables -P OUTPUT DROP
iptables -F OUTPUT
iptables -A OUTPUT -o lo -j ACCEPT
iptables -A OUTPUT -m conntrack --ctstate ESTABLISHED,RELATED -j ACCEPT
iptables -A OUTPUT -m owner --uid-owner 0 -j ACCEPT
```

- [ ] **Step 4: 跑测试确认通过**

Run: `node --experimental-test-module-mocks --test test/egress-proxy.test.ts`
Expected: PASS

- [ ] **Step 5: backend 集成 + wiring**

`wsl2-sandbox.ts`：`opts.egress` 存在时——provision 的 `handle.env` 注入代理三件套；ensureAgent 成功后经 /write 写入白名单、拉起代理（`setsid nohup node /opt/qm/egress-proxy.mjs &`）、exec `egress-lock.sh`（失败 → onError + provision 报错，fail-closed）。`buildWsl2`（wiring）经 `SANDBOX_EGRESS_PATH` env 读种子（proc.rs 在 Task 8 注入，指向 `payload/config/seeds/sandbox-egress.json`）；env 未设 → 不带 egress（开发机 local Docker 路径完全不受影响）。

- [ ] **Step 6: 真机验证（Windows + WSL2 已导入）**

guest 内按 spike 用例复验：白名单内代理 200 / 白名单外 403 / unset 后直连 DROP / 非 root flush 与 kill 均被拒。
Expected: 五项全中。

- [ ] **Step 7: Commit**

```bash
git add deploy/layers/meerkat/desktop/rootfs/egress-proxy.mjs deploy/layers/meerkat/desktop/rootfs/egress-lock.sh deploy/layers/meerkat/desktop/seeds/sandbox-egress.json src/sandbox/wsl2-sandbox.ts src/wiring.ts test/egress-proxy.test.ts
git commit -m "feat(desktop): in-guest egress proxy + iptables owner-match fence for wsl2"
```

### Task 11: agent 权限分离（AGENT_RUN_USER）+ 围栏验收

> 取代原"宿主机围栏分层"Task——spike 证明宿主机层不需要（`spike-win10-egress.md`）。权限分离是围栏成立的另一半：不降权，用户脚本是 root，可 flush iptables。

**Files:**
- Modify: `aws/microvm-agent/agent.mjs`（`AGENT_RUN_USER` 可选开关）
- Modify: `cli/templates/aws/microvm-agent/agent.mjs`（孪生副本同步，同一 commit）
- Modify: `deploy/layers/meerkat/desktop/rootfs/Dockerfile`（内置 `sandbox` 用户）
- Modify: `src/sandbox/wsl2-sandbox.ts`（拉起 agent 时注入 `AGENT_RUN_USER=sandbox`）
- Test: `test/agent-auth.test.ts` 追加降权用例

**Interfaces:**
- agent 契约新增：`AGENT_RUN_USER` env 非空时，`/exec` 的命令经 `setpriv --reuid=<uid> --regid=<gid> --clear-groups` 降权执行（uid/gid 在 agent 启动时由 `id -u <user>` 解析一次）；env 未设置时行为与现状完全一致（aws/local 不受影响）

- [ ] **Step 1: 失败测试**

daemon env 加 `AGENT_RUN_USER=nobody`：`/exec` body `{ cmd: "id -u" }` → stdout 断言 `65534`。不带该 env 的 daemon（Task 2 现有 daemon）→ `id -u` 在 win32 跳过数值断言（无 /bin/sh，只做 200 冒烟，纪律同 Task 2），Linux CI 断言 `0`。

- [ ] **Step 2: 跑测试确认失败**

Run: `node --experimental-test-module-mocks --test test/agent-auth.test.ts`
Expected: FAIL（新用例未实现）

- [ ] **Step 3: 实现**

agent.mjs 的 handleExec：解析一次 `AGENT_RUN_USER`（启动时 `id -u`/`id -g`），非空则 execFile 从 `/bin/sh -c cmd` 包为 `setpriv --reuid=U --regid=G --clear-groups /bin/sh -c cmd`。两份副本同改。Dockerfile 加 `useradd -r -s /usr/sbin/nologin sandbox`。wsl2 backend 的 agent 拉起 env 加 `AGENT_RUN_USER=sandbox`。

- [ ] **Step 4: 跑测试确认通过 + cli 一致性测试**

Run: `node --experimental-test-module-mocks --test test/agent-auth.test.ts`；`cd cli && node --experimental-test-module-mocks --test test/microvm-dockerfile.test.ts`
Expected: PASS

- [ ] **Step 5: 围栏五项真机复验**

spike 五项在真机沙箱环境（rootfs + agent + 代理全链路）复验一遍：白名单内 200 / 白名单外 403 / 绕过 DROP / 清规则拒 / 杀代理拒。

- [ ] **Step 6: Commit**

```bash
git add aws/microvm-agent/agent.mjs cli/templates/aws/microvm-agent/agent.mjs deploy/layers/meerkat/desktop/rootfs/Dockerfile src/sandbox/wsl2-sandbox.ts test/agent-auth.test.ts
git commit -m "feat(sandbox): AGENT_RUN_USER privilege separation for exec commands"
```

## Phase 5: 优化项与 Issue

### Task 12: 优化二——聊天页 🔒 锁标识

**Files:**
- Modify: `plugins/web-ui/src/composer.ts`（模型选择器渲染，`composer.ts:551-563`）
- Modify: `plugins/web-ui/src/core-bridge.ts`（fetch 锁状态）
- Modify: `plugins/web-ui/src/shell.ts`（锁状态注入 `applyRuntimeOptions` 流程，`shell.ts:823-833`）
- Test: 手动（Task 16 真机验收）+ 可能的 web-ui 单测（若有现成 harness）

**Interfaces:**
- Consumes: `GET /api/locks`（web-ui server 已有代理 → core `/v1/admin/session-locks`，`plugins/web-ui/server/index.ts:1139-1150`；桌面 principal `meerkat-desktop` 是 org_admin，可直接调）

**背景**：需求——锁生效（模型被会话锁覆写为本地/TRIZ 模型）后，模型选择器应禁用并显示 🔒 + 锁的模型名。锁状态现状未暴露到聊天 UI；`/api/locks` 返回 `SessionLock[]`（字段以 `src/core/orchestrator/types.ts:63` 为准，实现前先 `curl http://127.0.0.1:<webui>/api/locks` 确认字段名）。

- [ ] **Step 1: 锁状态获取**

`core-bridge.ts` 加：

```typescript
export interface SessionLockInfo { sessionId: string; harnessId: string; modelId: string; reason?: string }

export async function fetchSessionLocks(): Promise<SessionLockInfo[]> {
  const res = await fetch("/api/locks", { credentials: "same-origin" });
  if (!res.ok) return [];
  return (await res.json()) as SessionLockInfo[];
}
```

`shell.ts` 的 runtime-config 加载处并行 fetch，把"当前会话锁"放进与 `selectedModel` 同层的状态。

- [ ] **Step 2: 选择器锁定呈现**

`composer.ts` 模型 menuControl 处：当前会话有锁 → 渲染为禁用态按钮（不展开下拉），内容 `🔒 <lock.modelId>`，title 提示"此会话已锁定到本地隐私模型（会话锁），可在锁管理页解除"。无锁 → 现状不变。

- [ ] **Step 3: 手动验证**

起 dev 实例：对一个会话触发 L1 锁（发敏感内容）→ 模型选择器变 🔒 + `Meerkat-TRIZ-v1` 且不可切换；在 `/admin/locks` 释放锁 → 选择器恢复。

- [ ] **Step 4: Commit**

```bash
git add plugins/web-ui/src/composer.ts plugins/web-ui/src/core-bridge.ts plugins/web-ui/src/shell.ts
git commit -m "feat(web-ui): lock indicator on the model picker for session-locked chats"
```

### Task 13: 优化三——管理员界面入口

**Files:**
- Modify: `plugins/web-ui/server/index.ts`（runtime-config 代理响应补 `desktop` 标志，`:1301-1316`）
- Modify: `plugins/web-ui/src/shell.ts`（桌面模式显示入口）
- Test: 手动（Task 16）

**Interfaces:**
- Consumes: `/admin/locks` 页（ADR-009，`plugins/web-ui/server/locks.html`，路由 `index.ts:2277-2280`，仅 DESKTOP）
- Produces: `RuntimeConfig.desktop?: boolean`（core-bridge 接口同步加可选字段）

**背景**：`/admin/locks` 是孤儿页，无入口。桌面单用户 = 管理员，入口直接放聊天页。

- [ ] **Step 1: server 注入 desktop 标志**

`index.ts` 的 `GET /api/runtime-config` 代理处：relay 前若 `MEERKAT_DESKTOP` env 为真，给 JSON 加 `desktop: true`。

- [ ] **Step 2: 入口链接**

`shell.ts` 顶栏区域（参照现有外链模式 `chat.ts:1110-1114` 的 "View session log (admin)"）：`runtimeConfig.desktop === true` 时渲染「🔒 锁管理」链接 → `/admin/locks`。非桌面（RuntimeConfig 无 desktop 字段）不渲染。

- [ ] **Step 3: 手动验证**

桌面实例：入口可见、点击进入锁管理页、可 list/release；普通浏览器部署（无 MEERKAT_DESKTOP）无入口。

- [ ] **Step 4: Commit**

```bash
git add plugins/web-ui/server/index.ts plugins/web-ui/src/shell.ts plugins/web-ui/src/core-bridge.ts
git commit -m "feat(web-ui): admin locks entry point on desktop"
```

### Task 14: 优化一——模型列表添加 TRIZ 模型（定位 + 修复）

**Files:**
- Modify: `deploy/layers/meerkat/desktop/seeds/defaults.json`（预期主修）
- 视定位结果可能 Modify: `plugins/web-ui/src/model-options.ts` 或 `src/api/routes/surface.ts:1147-1163`

**背景**（探索结论，已定位 80%）：`webuiModels` 种子（`seeds/defaults.json:4`）只有 `["gpt-5.4-nano", "gemini-3.1-flash-lite"]`，setup 时写入 org allowlist（`index.ts:1050-1060`），`surface.ts:1147-1163` 按 allowlist 过滤 → TRIZ 模型被滤掉。剩余两道关卡需验证：`modelSupportedByHarness`（自定义 id 仅 pi/opencode/mock——我们 harness=pi，应过）与前端 `buildOption()` 的 `modelCatalog` meta（`model-options.ts:85-86`，meta 为 null 返回 null）。

- [ ] **Step 1: 改种子**

`seeds/defaults.json` 的 `webuiModels` 加 `"Meerkat-TRIZ-v1"`。

- [ ] **Step 2: dev 实例验证（决定是否有第二处修复）**

重新首启（或手工 `PUT /v1/admin/scopes/org:meerkat/webui-models` 补写后刷新）：`GET /api/runtime-config` 的 `modelsByHarness.pi` 是否含 `Meerkat-TRIZ-v1`；`modelCatalog` 是否有其 meta。选择器实际渲染：

- 列表出现且可选 → 完成，Step 4
- `modelsByHarness` 有但选择器不渲染 → `modelCatalog` 缺 meta：定位 catalog 组装（`surface.ts` 同函数附近），自定义 provider 模型应进 catalog——若现有逻辑只收内置注册表，修复 = 把已注册 custom provider 的模型并入 catalog（additive）
- `modelsByHarness` 没有 → 查 `serviceableModelIds`（provider key 可用性）与 `modelSupportedByHarness` 哪道滤掉，对症修复

- [ ] **Step 3: 隐私语义核对（必修）**

TRIZ 模型进选择器后，用户可**主动**把会话切到本地模型——这与会话锁的 L1 自动锁定不冲突（锁是敏感内容的强制路由，主动选择是用户权利）。但要确认：切到 TRIZ 后锁标识（Task 12）不误显示（无锁状态）；且 L1 内容在其他模型会话中仍正常触发锁。验证后发敏感内容一轮确认锁定行为不变。

- [ ] **Step 4: Commit**

```bash
git add deploy/layers/meerkat/desktop/seeds/defaults.json
git commit -m "feat(desktop): expose Meerkat-TRIZ-v1 in the model picker seed"
```

（若 Step 2 牵出第二处修复，拆独立 commit 并在信息里写清两道关卡。）

### Task 15: Issue 1——Create skill 报 Internal server error（定位 + 修复或转 v0.1.2）

**Files:**
- 定位前未知；入口已知：`plugins/web-ui/src/skills.ts:698-741`（saveCreate）→ `plugins/web-ui/server/index.ts:1341-1359`（代理）→ `src/api/routes/surface.ts:839-906`（createSkill）→ `src/api/app-skills.ts:365-397`（createOwnedSkill）

- [ ] **Step 1: 桌面 dev 实例复现**

`/dev-instance` 起桌面栈，web-ui 打开 Skills 页 → New skill → 填表提交 → 复现 500。

- [ ] **Step 2: 取根因堆栈**

core 日志（`DATA_DIR/logs/core.log` 或 dev 终端）找 `[server] 500 POST /v1/skills:`（`src/api/server.ts:302-303`）——堆栈即根因。按探索结论，可疑点排序：`sharedSkillCreateBlock` capability 门禁（`surface.ts:43`）、`app.createOwnedSkill` 的 `withSkillMutationLock`、`skills.review()/publish()` 链。

- [ ] **Step 3: 修复或转 v0.1.2**

根因明确且修复 ≤0.5 人日 → 本 Task 内修（TDD：先写复现测试）。修复面大或触及 skills 核心逻辑 → 记录根因到 Issue #1 并转 v0.1.2，本 Task 以"定位报告"收尾（报告进 `docs/meerkat/v0.1.1/` 或 Issue 评论）。

- [ ] **Step 4: Commit（若有修复）**

```bash
git commit -m "fix(core): create skill internal error — <root cause>"
```

---

## Phase 6: 真机验收与文档

### Task 16: 三路径真机验收 + ADR/文档/CHANGELOG

**Files:**
- Modify: `docs/meerkat/v0.1.1/ADR.md`（新增：ADR-? 沙箱出网不放行公网的否决记录、agent token 内核缝、wsl2 底座选型）
- Create: `docs/meerkat/v0.1.1/发布文档.md`、`docs/meerkat/v0.1.1/用户文档.md`（对齐 v0.1 同名文档结构）
- Modify: `docs/meerkat/v0.1.1/CHANGELOG.md`

- [ ] **Step 1: 全新 Win11/Win10（未启用 WSL2）路径**

装包 → 首启 → 设置页沙箱区块"未启用" → [启用沙箱] → UAC → 提示重启 → 重启 → 启动清单沙箱行黄灯"初始化中" → 绿灯 → 对话中触发 execute 工具 → 文件落在 guest workspace。同时验证 Task 12/13/14 的 UI。

- [ ] **Step 2: 已启用 WSL2 路径**

直接首启 → 黄灯"初始化中"（秒级~30s）→ 绿灯 → execute 可用。升级场景：改 fingerprint 重打包 → 启动显示"更新中" → 完成后绿灯。

- [ ] **Step 3: 降级路径**

无虚拟化虚拟机（或 BIOS 关 VT-x）/ OS build < 19041 → 沙箱行"此机器不支持沙箱"，对话/路由完整，execute 工具报清晰错误。用户拒绝 UAC → 设置页标注"沙箱不可用"。

- [ ] **Step 4: 围栏验证（设计文档第十节）**

白名单内 `pip install cowsay` 成功；白名单外域名代理 403；`unset HTTP_PROXY` 后直连被拦（分支 A）或全部出网超时（分支 B 无网络档）。

- [ ] **Step 5: 文档收尾**

ADR：①否决"沙箱默认放行公网"（隐私承诺链论证，防回退）②agent.mjs token 认证内核缝（env 可选、默认关闭，对齐 v0.1 三处内核缝先例）③WSL2 选型（POSIX 契约证据链）。用户文档：沙箱能力说明 + 启用步骤 + 降级 FAQ + rootfs 更新丢 workspace 的说明。CHANGELOG：按 v0.1 格式列全部条目。

- [ ] **Step 6: Commit**

```bash
git add docs/meerkat/v0.1.1
git commit -m "docs(meerkat): v0.1.1 sandbox acceptance, ADR, user and release docs"
```

---

## 依赖与执行顺序

```text
Task 0 (spike) ✅ 已完成（2026-08-17，结论：guest 内围栏，见 spike-win10-egress.md）
Task 1 ✅ → Task 2 ✅ → Task 3 → Task 4 ──▶ Task 10 ─▶ Task 11
Task 5 → Task 6 → Task 7 → Task 8 → Task 9 ──────────────▶ Task 16
Task 12 / 13 / 14 / 15 相互独立，与 Phase 1-4 无耦合 ──────▶ Task 16
```

Task 0 已完成且形状确定；Task 3-4 与 Task 5-9 可并行；Phase 5 四个任务随时插入。估算合计 15-23 人日（spike 后 Task 10-11 不确定性消除，较原估 16-25 下调）。
